# 发布误判修复与 4003 验证计划

依据：2026-09-20 对 4002 近期 164 条发布任务的只读审计。
实施范围：developer 分支、4003、short-video-analyzer-dev。4002 保持原样。

## 修复与验收

1. 商品绑定（已证实漏识别）：排除弹窗、描述编辑器中的同名文字后比较编辑页标签；完整 title 可验证视觉截断标签。商品仍按完整 ID 选择，不能用相似名称判成功。回放弹窗内同名文字消失、标签出现、错误商品 ID、仅描述同名、隐藏标签。
2. 上传状态（疑似误判）：识别文件已选、上传加载、编辑页就绪和明确失败；控件消失本身不是成功。加载时等待编辑页，不重复选文件。仅确认仍在空闲选择页时使用备用文件选择器。回放加载超过旧 5 秒门槛、替换 input、明确上传失败、空白页、CDP 异常但已加载、备用流程。
3. 提交确认：将 Continue to post? 明确标为待人工确认，保留原定时意图，不自动点击 Post now，也不自动重发。只有内容管理页或明确发布/排程成功提示算提交成功，“Uploaded/上传成功”不算。回放确认弹窗、无回执、成功提示、错误页和非目标域名。
4. 诊断：每次浏览器执行使用独立 attempt 目录，记录异常链，重试不覆盖原截图。既有任务状态及发布记录不迁移、不批量改为成功。

## 验证及发布

- 使用现有 Playwright 和临时页面、临时数据库；阻断测试浏览器外网请求，不调用 TikTok/SociaVault/LLM。
- 在 4003 Compose analyzer 中运行商品绑定、描述、提交及新增上传状态回归。
- 通过 GitHub 同步 developer，确认 4003 无活动发布/采集任务后只重启开发 web，检查 /healthz、/proxy 与部署提交。
- 本轮不真实上传/提交 TikTok，不声称已完成真实账号端到端验证。正式迁移前需要选定账号，进行保留定时语义的受控验收。

## 回滚

仅回退本次 developer 提交并同步 GitHub，随后重启 4003 web；无需数据库回滚，不触碰 4002。
