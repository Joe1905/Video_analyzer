# UI 4004 开发交付

本文件将已确认的新视觉方案直接映射到真实路由。它是本次实现的设计依据；旧 Figma AI 对话稿不参与本轮开发。

## 视觉基准

- 雾白工作台：`#f5f6f8` 画布、白色内容层、低对比描边与克制阴影。
- 中文优先；Home、SellerSprite、出海匠、Shop、Proxy 等产品名保留。
- 首页使用符号化首页导航；操作优先用图标、原生 `title` 提示与可见焦点。
- 信息按“当前动作 → 结果 → 细节”组织，隐藏没有实际价值的说明文案。
- Header 260ms 渐显，主体 400ms 渐显上移，弹窗 220ms；`prefers-reduced-motion` 下全部降级。

## 页面映射

| 新版区域 | 真实路由 | 模板 | 重点状态 |
| --- | --- | --- | --- |
| Home / SellerSprite / 出海匠 | `/`、`/amazon`、`/chuhaijiang` | `scripts/static/chat.html` | 会话、请求中、空会话、工具成功/失败 |
| 邻聊 | `/lan-chat` | `scripts/static/lan_chat.html` | 登录、频道/私聊/群组、成员、附件、图片预览 |
| 日报 | `/report` | `scripts/static/report.html` | 默认、运行中、空日报、失败、视频详情弹窗 |
| 视频播放分析 | `/report/player` | `scripts/static/report_player.html` | 加载、播放器、时间段解析、总体拆解 |
| Shop / 数据 | `/shop`、`/metrics` | 对应 `scripts/static/*.html` | 表单、加载、结果、错误 |
| 视频分析 / 图片工具 | `/extract`、`/tool` | `scripts/web_index.html`、`scripts/static/tool.html` | 上传、队列、预览、分析结果、错误 |
| Proxy 账号与 IP | `/proxy` | `scripts/static/proxy.html` | IP 池、飞书头像账号、登录观测、发布/采集、弹窗 |

## 尺寸与可访问性

- 桌面：1280px 以上按工作台布局展示。
- 移动：390px 起收敛为单列，不允许页面级横向溢出；触控区域不小于 44px。
- 所有页面沿用既有 DOM ID、Fetch/SSE 地址和事件绑定。视觉层由 `/assets/ui-system.css` 注入。
- 模态、抽屉、图片预览均继承统一遮罩、圆角、焦点和动效规则。

## 4004 隔离预览

- 工作树：`/home/openclaw/Video_analyzer-ui-4004`
- Compose 项目：`short-video-analyzer-ui-4004`
- 镜像：`short-video-analyzer-ui-4004:latest`
- 端口：`4004`
- 数据：该工作树下的 `data-dev`、`videos-dev`、`output-dev`
- 定时日报关闭；Proxy 辅助端口：`19100–19199`

部署脚本：`bash scripts/deploy_ui_4004.sh`。先将 `.env.ui-4004.example` 复制为服务器私有的 `.env.ui-4004`，并只补齐运行必需的现有 API 配置。
